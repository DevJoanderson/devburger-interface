// simula os dados do produto
const product = {
  name: "X-Burguer",
  price: "R$ 25,00",
  product_url: "http://localhost:3001/product-file/abc123"
};

// simula o uso do produto (como se fosse no React)
console.log("Nome:", product.name);
console.log("Preço:", product.price);
console.log("URL:", product.product_url);
